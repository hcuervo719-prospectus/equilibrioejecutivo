[build]
  command = "npm run build"
  publish = "dist"

# Optional: specify functions folder if you later add serverless functions
# functions = "netlify/functions"

[build.environment]
  NODE_VERSION = "18"

[context.production]
  command = "npm run build"
  publish = "dist"
